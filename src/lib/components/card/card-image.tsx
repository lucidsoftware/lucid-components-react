import styled from '../../../theme/styled';

const CardImage = styled('div')`
  flex: 1 1 auto;
  margin: 0;
  padding: 0;
  img {
    width: 100%;
    height: auto;
  }
`;

export default CardImage;
